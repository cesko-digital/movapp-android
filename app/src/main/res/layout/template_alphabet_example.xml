<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/text_view_alphabet_example"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="This is a member template"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@+id/image_play_sound_alphabet_example"
        android:textSize="16sp" />


    <ImageView
        android:id="@+id/image_play_sound_alphabet_example"
        android:layout_width="@dimen/playSize"
        android:layout_height="@dimen/playSize"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"

        android:contentDescription="play transcription"
        app:srcCompat="@drawable/ic_play" />
</RelativeLayout>